#include "main.h"
/**
 * mul - returns a product of two integers
 * @a: the first integer
 * @b: the second integer
 * Return: product of two integer, multiplied
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
